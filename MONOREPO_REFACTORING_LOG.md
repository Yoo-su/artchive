# 📘 모노레포 구조 개선 및 빌드 정상화 기록 (최종본)

본 문서는 Bookjeok 모노레포의 패키지 의존성 꼬임 문제를 해결하고, 차세대 프레임워크(Next.js 15) 전환 과정에서의 빌드 무결성을 확보하기 위해 진행된 리팩토링 과정을 상세히 기록합니다.

## 1. 개요 및 배경 (Context)

### 초기 문제 상태
- **복잡한 서브패스 참조**: `@bookjeok/core/auth`, `@bookjeok/api-client/book` 등 파편화된 익스포트로 인한 의존성 관리 난이도 상승.
- **Next.js 15/React 19 호환성**: 최신 프레임워크 도입으로 인한 기존 React 18 기반 코드의 타입 파손 및 빌드 중단.
- **런타임 초기화 결함**: 모노레포 패키지(react-query 등) 내의 초기화 순서 문제로 인한 `undefined` 참조 에러 발생.

---

## 2. 해결 전략 및 접근 방식 (Strategy)

### 2.1 패키지 노출 인터페이스 표준화
- **공유 패키지 루트 익스포트 단일화**: 모든 외부 노출은 오직 패키지 루트 엔트리 포인트(`@bookjeok/core` 등)를 통해서만 수행하도록 강제.
- **의존성 격리**: `peerDependencies` 정리를 통해 컨슈머 앱과의 라이브러리 중복 및 버전 충돌 원천 차단.

### 2.2 타입 시스템 안정화 (Next.js 15 대응)
- **React 타입 버전 고정**: `pnpm.overrides`를 통해 `@types/react` 버전을 `18.3.1`로 고정하여 하이브리드 환경(Next.js 15 + React 18 코드)에서의 타입 무결성 확보.
- **위험 구문 우회**: `Switch`, `RefObject` 등 React 19에서 파괴적 변경이 일어난 지점에 대해 `any` 타입을 사용한 안전한 브릿지 적용.

---

## 3. 주요 기술적 조치 사항 (Implementation)

### 3.1 공유 패키지 리팩토링
- **익스포트 순서 최적화**: 배럴 파일(`index.ts`) 내에서 `query-keys`를 최상단에 배치하여 소비자(`queries`, `mutations`)가 참조 시점에 항상 초기화되어 있도록 보장.
- **팩토리 정의 표준화**: `query-key-factory` 사용 시 `null` 지시어 방식을 채택하여 런타임 가용성 대폭 향상.

### 3.2 빌드 파이프라인 정비
- `tsup` 빌드 결과물에 `"use client";` 지시어를 자동 주입하도록 `banner` 설정 추가.
- `tsconfig.base.json`에 `moduleResolution: "Bundler"`를 적용하여 현대적 모듈 해석 체계 도입.

---

## 4. 트러블슈팅 로그 (Troubleshooting)

| 현상 | 원인 파악 | 조치 결과 |
| :--- | :--- | :--- |
| **`pnpm build` 실패** | React 19의 엄격한 타입 체크 및 라이브러리 미지원 | `pnpm.overrides`를 통한 타입 버전 롤백 및 우회 코드 적용 |
| **`queryKey` undefined 에러** | 패키지 내 배럴 파일 익스포트 순서 꼬임으로 인한 초기화 지연 | `query-keys` 우선 노출 및 정의 방식 표준화로 해결 |
| **서버 `params.id` 타입 에러** | Express v5+의 엄격한 타입 추론 | 타입 가드(isArray 체크) 추가로 안정성 확보 |

---

## 5. 향후 유지보수를 위한 룰 (Guidelines)

1. **임포트 규칙**: 새로운 기능을 추가할 때 반드시 패키지 루트(`@bookjeok/core`)에서 가져오며, 서브패스는 더 이상 사용하지 않습니다.
2. **패키지 내부 참조**: 같은 패키지 내의 소스를 참조할 때는 **무조건 상대 경로**를 사용해야 순환 참조를 막을 수 있습니다.
3. **기술 부채 상환**: 임시 적용된 `any` 우회 코드들은 차후 UI 라이브러리가 React 19를 완벽 지원할 때 원래 타입으로 복구해야 합니다.

---

**결과 보고**: 현재 루트 빌드(`pnpm build`)는 전 프로젝트에 대해 **성공(PASS)** 상태이며, 모든 페이지의 정적 데이터 생성(Prefetch)이 정상 작동합니다.
